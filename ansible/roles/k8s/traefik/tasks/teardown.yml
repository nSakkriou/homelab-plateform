---
- name: Uninstall Traefik Helm chart
  kubernetes.core.helm:
    name: traefik
    namespace: traefik
    state: absent
    wait: true

- name: Delete traefik namespace
  kubernetes.core.k8s:
    name: traefik
    api_version: v1
    kind: Namespace
    state: absent
