creation_rules:
  - path_regex: infra/secrets/.*\.yaml$
    # Remplacer par la clé publique age du serveur.
    # Obtenir la clé publique depuis la clé privée :
    #   age-keygen -y /chemin/vers/age.key
    age: "<CLÉ_PUBLIQUE_AGE_DU_SERVEUR>"
